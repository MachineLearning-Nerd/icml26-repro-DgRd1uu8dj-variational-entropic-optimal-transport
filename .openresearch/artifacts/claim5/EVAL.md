# Claim 5 evaluation contract

Run the inherited command `uv sync --frozen && .venv/bin/python -m vareot_repro.run_all` on HF `cpu-upgrade`. The profile node estimates full-run cost without changing the official tensor shape. A descendant changes only committed `steps` and `profile_only` values, then reruns Claims 1–4 and the final Claim 5 gate.
